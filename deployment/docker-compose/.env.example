# Docker Compose Environment Configuration
# Copy this file to .env and customize as needed

# ==========================================
# Tor Configuration
# ==========================================

# Tor SOCKS5 proxy port (default: 9050)
TOR_SOCKS_PORT=9050

# Tor control port (default: 9051)
TOR_CONTROL_PORT=9051

# Tor log level (notice, info, debug)
TOR_LOG_LEVEL=notice

# ==========================================
# Gethrelay Configuration
# ==========================================

# Network ID (1 = mainnet, 3 = ropsten, etc.)
GETH_NETWORK_ID=1

# Verbosity level (0=silent, 1=error, 2=warn, 3=info, 4=debug, 5=trace)
GETH_VERBOSITY=3

# Maximum number of network peers (default: 50)
GETH_MAX_PEERS=50

# Enable metrics collection (true/false)
GETH_METRICS=false

# Metrics HTTP server (if enabled)
GETH_METRICS_ADDR=127.0.0.1
GETH_METRICS_PORT=6060

# ==========================================
# Peer Manager Configuration
# ==========================================

# Interval between peer discovery checks (in seconds)
PEER_CHECK_INTERVAL=30

# Maximum retries for RPC connection
PEER_MANAGER_MAX_RETRIES=12

# Retry delay between connection attempts (in seconds)
PEER_MANAGER_RETRY_DELAY=5

# ==========================================
# RPC Configuration
# ==========================================

# HTTP RPC bind address (SECURITY: should always be 127.0.0.1 for localhost-only access)
GETH_HTTP_ADDR=127.0.0.1

# HTTP RPC port (default: 8545)
GETH_HTTP_PORT=8545

# HTTP RPC API modules (comma-separated)
# Recommended: admin,eth,net
# DO NOT expose: personal, accounts (security risk)
GETH_HTTP_API=admin,eth,net

# ==========================================
# Docker Configuration
# ==========================================

# Container restart policy (no, always, on-failure, unless-stopped)
RESTART_POLICY=unless-stopped

# Docker network subnet
DOCKER_SUBNET=172.28.0.0/16

# ==========================================
# Development/Debugging
# ==========================================

# Enable pprof profiling endpoint (development only)
# GETH_PPROF=false
# GETH_PPROF_ADDR=127.0.0.1
# GETH_PPROF_PORT=6060

# Enable development mode with additional logging
# DEV_MODE=false
